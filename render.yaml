services:
  - type: web
    name: portfolio-ivan-valdez
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: "3000"
      - key: GEMINI_API_KEY
        sync: false
